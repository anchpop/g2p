[package]
name = "g2p-types"
version = "0.4.0"
edition = "2024"
description = "Shared pronunciation label types without a phonemizer engine"
repository = "https://github.com/anchpop/g2p"
license = "GPL-3.0-or-later"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
